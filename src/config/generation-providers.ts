/**
 * Project-owned generation provider registrations.
 *
 * BeatAPI is registered by the template itself. Adopters can import their own
 * server-only adapter here and register it with `registerGenerationProvider`.
 * Keep provider credentials in server environment variables or encrypted
 * admin configuration; never expose them through VITE_ variables.
 */
export const registerProjectGenerationProviders = () => {
  // Intentionally empty in the upstream template.
};
